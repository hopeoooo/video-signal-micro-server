server:
  port: 7300

spring:
  application:
    name: risk-management-center
  cloud:
    nacos:
      discovery:
        namespace: ${zlt.naocs.server.namespace}
#        metadata:
#          version: zlt