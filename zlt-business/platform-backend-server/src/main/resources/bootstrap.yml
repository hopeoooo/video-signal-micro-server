server:
  port: 6000

spring:
  application:
    name: platform-backend-server
  cloud:
    nacos:
      discovery:
        namespace: ${zlt.naocs.server.namespace}
#        metadata:
#          version: zlt