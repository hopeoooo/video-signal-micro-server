server:
  port: 6000

spring:
  application:
    name: platform-backend-server
  cloud:
    nacos:
      discovery:
        namespace: ${zlt.mynaocs.namespace}
#        metadata:
#          version: zlt