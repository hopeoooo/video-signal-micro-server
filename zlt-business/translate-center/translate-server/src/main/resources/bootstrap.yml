server:
  port: 7700

spring:
  application:
    name: translate-center
  cloud:
    nacos:
      discovery:
        namespace: ${zlt.naocs.server.namespace}
#        metadata:
#          version: zlt