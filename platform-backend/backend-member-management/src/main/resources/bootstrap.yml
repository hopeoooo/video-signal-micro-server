server:
  port: 6002

spring:
  application:
    name: platform-member-management
  cloud:
    nacos:
      discovery:
        namespace: ${zlt.naocs.server.namespace}
#        metadata:
#          version: zlt