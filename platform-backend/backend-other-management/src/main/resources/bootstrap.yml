server:
  port: 6003

spring:
  application:
    name: platform-other-management
  cloud:
    nacos:
      discovery:
        namespace: ${zlt.naocs.server.namespace}
#        metadata:
#          version: zlt